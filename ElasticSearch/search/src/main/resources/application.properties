spring.application.name=elasticsearch-service

spring.elasticsearch.uris=${SPRING_ELASTICSEARCH_URIS:http://localhost:9200}
spring.elasticsearch.connection-timeout=5s
spring.elasticsearch.socket-timeout=30s

server.port=8080

logging.level.org.springframework.data.elasticsearch=DEBUG
logging.level.nais.search=DEBUG

spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=false

eureka.client.enabled=${EUREKA_CLIENT_ENABLED:true}
eureka.client.service-url.defaultZone=${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka}

spring.data.redis.host=${SPRING_DATA_REDIS_HOST:localhost}
spring.data.redis.port=${SPRING_DATA_REDIS_PORT:6379}

app.redis.book-prefix=es:book:
app.redis.person-prefix=es:person:

ocr.tessdata-path=${OCR_TESSDATA_PATH:/usr/share/tesseract-ocr/5/tessdata}
ocr.language=${OCR_LANGUAGE:eng}
ocr.max-pages=${OCR_MAX_PAGES:20}

spring.servlet.multipart.max-file-size=${OCR_MAX_FILE_SIZE:25MB}
spring.servlet.multipart.max-request-size=${OCR_MAX_REQUEST_SIZE:25MB}
